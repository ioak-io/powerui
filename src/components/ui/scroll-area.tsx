import { ScrollArea as ScrollAreaPrimitive, ScrollBar } from "@/components/ui-library/scroll-area";

const ScrollArea = ScrollAreaPrimitive;
const ScrollBarWrapper = ScrollBar;

export { ScrollArea, ScrollBarWrapper as ScrollBar };
