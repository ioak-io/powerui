import { Popover as PopoverPrimitive, PopoverTrigger, PopoverContent } from "@/components/ui-library/popover";

const Popover = PopoverPrimitive;
const PopoverTriggerWrapper = PopoverTrigger;
const PopoverContentWrapper = PopoverContent;

export { Popover, PopoverTriggerWrapper as PopoverTrigger, PopoverContentWrapper as PopoverContent };
