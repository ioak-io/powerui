import {
  Sheet as SheetPrimitive,
  SheetPortal,
  SheetOverlay,
  SheetTrigger,
  SheetContent,
  SheetHeader,
  SheetFooter,
  SheetTitle,
  SheetDescription,
  SheetClose,
} from "@/components/ui-library/sheet";

const Sheet = SheetPrimitive;
const SheetPortalWrapper = SheetPortal;
const SheetOverlayWrapper = SheetOverlay;
const SheetTriggerWrapper = SheetTrigger;
const SheetContentWrapper = SheetContent;
const SheetHeaderWrapper = SheetHeader;
const SheetFooterWrapper = SheetFooter;
const SheetTitleWrapper = SheetTitle;
const SheetDescriptionWrapper = SheetDescription;
const SheetCloseWrapper = SheetClose;

export {
  Sheet,
  SheetPortalWrapper as SheetPortal,
  SheetOverlayWrapper as SheetOverlay,
  SheetTriggerWrapper as SheetTrigger,
  SheetContentWrapper as SheetContent,
  SheetHeaderWrapper as SheetHeader,
  SheetFooterWrapper as SheetFooter,
  SheetTitleWrapper as SheetTitle,
  SheetDescriptionWrapper as SheetDescription,
  SheetCloseWrapper as SheetClose,
};
