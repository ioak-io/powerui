import { Avatar as AvatarPrimitive, AvatarImage, AvatarFallback } from "@/components/ui-library/avatar";

const Avatar = AvatarPrimitive;
const AvatarImageWrapper = AvatarImage;
const AvatarFallbackWrapper = AvatarFallback;

export { Avatar, AvatarImageWrapper as AvatarImage, AvatarFallbackWrapper as AvatarFallback };
