import { Tooltip as TooltipPrimitive, TooltipTrigger as TooltipTriggerPrimitive, TooltipContent as TooltipContentPrimitive, TooltipProvider as TooltipProviderPrimitive } from "@/components/ui-library/tooltip";

const Tooltip = TooltipPrimitive;
const TooltipTrigger = TooltipTriggerPrimitive;
const TooltipContent = TooltipContentPrimitive;
const TooltipProvider = TooltipProviderPrimitive;

export {
  Tooltip,
  TooltipTrigger,
  TooltipContent,
  TooltipProvider,
};
